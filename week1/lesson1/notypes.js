function printPerson(person) {
  console.log(`${person.name} is ${person.age} years old`);
}

const person = {
  name: 'John',
  age: 20,
};

// printPerson(person);
// printPerson(20); // what will happen here?
